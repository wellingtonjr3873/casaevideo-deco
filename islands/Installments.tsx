import Component from "deco-sites/casaevideo/components/product/Installments.tsx"; 
import type { Props } from "deco-sites/casaevideo/components/product/Installments.tsx";

const Island = (props: Props) => {
    return <Component {...props}/>
}

export default Island