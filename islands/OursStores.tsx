import Component from "$store/components/product/MountedPDP/OursStores/index.tsx"
import type { Props } from "$store/components/product/MountedPDP/OursStores/index.tsx";

const Island = (props: Props) => {
    return <Component {...props}/>
}

export default Island