import Component from "$store/components/ui/MiniBannerCarousel.tsx";
import type { Props } from "$store/components/ui/MiniBannerCarousel.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
