import Component from "$store/components/header/SentryConfig.tsx";
import type { Props } from "$store/components/header/SentryConfig.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
