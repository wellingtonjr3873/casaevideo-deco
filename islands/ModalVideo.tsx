import Component from "$store/components/ui/ModalVideo/ModalVideo.tsx";
import type { ModalVideoProps } from "$store/components/ui/ModalVideo/ModalVideo.tsx";

function Island(props: ModalVideoProps) {
  return <Component {...props} />;
}

export default Island;
